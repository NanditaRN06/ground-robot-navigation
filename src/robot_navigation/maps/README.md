# Localization maps

Place the saved SLAM output for the project in this directory.

Recommended file naming:
- `replace_with_saved_map.yaml`
- `replace_with_saved_map.pgm`

The `map_server.yaml` configuration points to the YAML file above by default, so once the team saves the map, they only need to replace the placeholder files with the real generated map pair.
